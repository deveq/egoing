/**
 * 변수 명명 규칙
 * 1. 변수명에는 공백 불가
 * 2. 첫 글자로 _와 $가 가능, 그 이후로는 문자,숫자, 밑줄, 달러만 가능
 * 3. 대소문자 구분함
 * 4. 자바스크립트 예약어 사용 불가(class, return, true, false 등..)
 */
var a = 1;
console.log(a);

a = 2;
console.log(a);

// 문자열과 변수를 이을 때
"문자열" + str + "문자열";
